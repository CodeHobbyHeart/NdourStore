/*document.addEventListener("DOMContentLoaded", () => {
    // Sélectionner toutes les images des articles
    const images = document.querySelectorAll(".gallery .item img");
  
    // Ajouter un écouteur pour chaque image
    images.forEach((img) => {
      img.addEventListener("mouseover", () => {
        img.style.transform = "scale(1.2)"; // Appliquer un zoom
        img.style.transition = "transform 0.3s ease"; // Transition fluide
      });
  
      img.addEventListener("mouseout", () => {
        img.style.transform = "scale(1)"; // Retour à la taille normale
      });
    });
  });
  */
